services:
  db:
    image: postgres:16
    container_name: emissions_db
    environment:
      POSTGRES_DB: emissions
      POSTGRES_USER: emissions_user
      POSTGRES_PASSWORD: password
      POSTGRES_HOST_AUTH_METHOD: trust
    ports:
      - "5432:5432"